#loop through values and keys

info = {"name": "Jane", "age": "48", "favefood": "icecream"} #dictionary structure: "key": "value"

for key, value in info.items():
    print("{}: {}" .format(key, value))
