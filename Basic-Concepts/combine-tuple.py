"""
Steps:
    (1) make two tuples
    (2) combine them into one tuple
"""

nset1 = (1, 2, 3)
nset2 = (4, 5, 6)

combined_nset = nset1 + nset2 #concatenate tuples like you would combine numbers / strings

print(nset1)
print(nset2)
print(combined_nset)
